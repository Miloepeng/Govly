# Base requirements
-r requirements.txt

# Authentication dependencies
supabase==2.0.2
PyJWT>=2.8.0,<3.0.0

# File upload dependencies
python-multipart>=0.0.6

# OCR dependencies
pytesseract==0.3.10
pdf2image==1.16.3
Pillow==10.0.1

# Additional system packages needed (install via apt-get in Dockerfile):
# - tesseract-ocr
# - tesseract-ocr-vie
# - poppler-utils
